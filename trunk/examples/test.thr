theory Nat =
thy
  set nat

  const f : nat -> nat
  const x : nat
  const y = f ( f ( x ) )
end

theory Real(N : Nat) =
thy
  set real
  const inject : N.nat -> real
end

theory Foo =
thy
  model N : Nat
  model R : Real
end