theory Real =
thy
  set r
  relation apart : r * r

  constant z : r
  constant e : r
  constant p : r * r -> r
  constant m : r * r -> r
  constant n : r * r -> r

  set r_nonzero = { x : r | apart (x, z) }

  const d : r * r_nonzero -> r

end