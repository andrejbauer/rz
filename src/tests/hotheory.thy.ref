[Processing ./hotheory.thy]
module type T =
 sig
   type a
    
   (**{v predicate (=a=) : a -> a -> bool v}*)
   (**  {v assertion symmetric_a :  forall x:a, y:a,  x =a= y -> y =a= x v}
         
        {v assertion transitive_a : 
          forall x:a, y:a, z:a,  x =a= y /\ y =a= z -> x =a= z v}
   *)
    
   (**{v predicate ||a|| : a -> bool v}*)
   (**  {v assertion support_def_a :  forall x:a,  x : ||a|| <-> x =a= x v}
   *)
 end
 
module type F = functor (M : T) ->
                sig
                  
                end
 
module type G = functor (M : T) ->
                sig
                  
                end
 
module type H =
 functor (M : T) ->
 sig
   val x : M.a
   (**  {v assertion x_support :  x : ||M.a|| v}
   *)
 end
 
module M0 : T
 
module G0 : G
 
val x : 'a
(**  {v assertion 'a [(=a=):'a -> 'a -> bool, ||a||:'a -> bool] x_support : 
       (forall y:'a, z:'a,  y =a= z -> z =a= y) ->
       (forall y:'a, z:'a, w:'a,  y =a= z /\ z =a= w -> y =a= w) ->
       (forall y:'a,  y : ||a|| <-> y =a= y) -> x : ||'a|| v}
*)
 
module type PSI = functor (X : G) ->
                  sig
                    
                  end
 
module type RHO = functor (X : H) ->
                  sig
                    
                  end
 
module type PHI = functor (X : G) ->
                  sig
                    
                  end
 
module Q : sig
             
           end
 
module R : sig
             
           end
 
module W : sig
             
           end

